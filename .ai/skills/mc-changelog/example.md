<!--
Starter for a hand-authored release changelog. Copy this to
`changelogs/<version>.md` in a mod repo (version = the release tag minus `v`,
e.g. `changelogs/1.0.0.md`), then replace the content below. When present, this
file is published VERBATIM to GitHub, Modrinth, and CurseForge and the
AI-generated notes are skipped. Delete this comment before committing — it ships
as-is. See mc-changelog/SKILL.md for what belongs here and what does not.
-->
One or two sentences on what this release is about — the one thing to know before
the bullets. Lead with any action players must take (config reset, world backup,
a now-incompatible dependency).

## Added
- New content or features, described as they appear in-game.

## Changed
- Behavior that works differently now — the outcome, not the code change.

## Fixed
- A bug players actually hit, in their words.
