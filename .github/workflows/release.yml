# Thin stub — logic lives in rfizzle/concord (mod-release.yml).
name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write
  pull-requests: write

jobs:
  release:
    uses: rfizzle/concord/.github/workflows/mod-release.yml@master
    with:
      modrinth-id: "8KuQhMGI"
      curseforge-id: "1546072"
    secrets: inherit
